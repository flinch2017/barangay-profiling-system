{
  "name": "barangay-profiling-system",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && node scripts/copy-frontend-dist.mjs",
    "start": "npm --prefix backend start",
    "backend:install": "npm --prefix backend install",
    "backend:start": "npm --prefix backend start",
    "frontend:build": "npm --prefix frontend install && npm --prefix frontend run build && node scripts/copy-frontend-dist.mjs"
  }
}
