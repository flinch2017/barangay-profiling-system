import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { apiUrl } from "../lib/api";
import "../styles/claimProfile.css";

const fields = ["first_name", "middle_name", "last_name", "suffix", "contact_number", "email", "birthdate", "gender", "civil_status", "street", "purok", "occupation", "salary", "educational_level", "school_name", "year_level", "course", "currently_enrolled", "graduation_year", "fourps_beneficiary", "senior_citizen", "voter"];

export default function ResidentProfileUpdate() {
  const navigate = useNavigate();
  const [form, setForm] = useState(null);
  const [message, setMessage] = useState("");
  const [saving, setSaving] = useState(false);
  useEffect(() => { fetch(apiUrl("/api/resident-claims/me"), { headers: { Authorization: `Bearer ${localStorage.getItem("token")}` } }).then((response) => response.json()).then((data) => { if (!data.resident) throw new Error("Claim a resident profile before requesting an update"); setForm(Object.fromEntries(fields.map((field) => [field, data.resident[field] ?? ""]))); }).catch((error) => setMessage(error.message)); }, []);
  const change = (event) => { const { name, value, type, checked } = event.target; setForm((current) => ({ ...current, [name]: type === "checkbox" ? checked : value })); };
  async function submit(event) { event.preventDefault(); setSaving(true); setMessage(""); try { const response = await fetch(apiUrl("/api/resident-claims/profile-updates"), { method: "POST", headers: { Authorization: `Bearer ${localStorage.getItem("token")}`, "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setMessage(data.message); } catch (error) { setMessage(error.message); } finally { setSaving(false); } }
  if (!form) return <main className="claim-profile-page"><p>{message || "Loading your profile..."}</p></main>;
  return <main className="claim-profile-page"><section className="claim-page-heading"><button className="claim-back" onClick={() => navigate("/resident/portal")}>← Return to portal</button><span className="claim-kicker">PROFILE UPDATE</span><h1>Request profile changes</h1><p>Your barangay administrator must review and approve this request before your profile is updated.</p></section>{message && <div className="claim-alert">{message}</div>}<form className="claim-form-card" onSubmit={submit}><div className="claim-form-intro"><span>REVIEW REQUIRED</span><strong>Proposed profile details</strong></div><Section title="Personal information"><Field label="First name"><input name="first_name" value={form.first_name} onChange={change} required /></Field><Field label="Middle name"><input name="middle_name" value={form.middle_name} onChange={change} /></Field><Field label="Last name"><input name="last_name" value={form.last_name} onChange={change} required /></Field><Field label="Suffix"><input name="suffix" value={form.suffix} onChange={change} /></Field><Field label="Birthdate"><input name="birthdate" type="date" value={form.birthdate} onChange={change} /></Field><Field label="Gender"><select name="gender" value={form.gender} onChange={change}><option value="">Not recorded</option><option>Male</option><option>Female</option><option>Nonbinary</option></select></Field><Field label="Civil status"><input name="civil_status" value={form.civil_status} onChange={change} /></Field></Section><Section title="Contact and address"><Field label="Contact number"><input name="contact_number" value={form.contact_number} onChange={change} /></Field><Field label="Email"><input name="email" type="email" value={form.email} onChange={change} /></Field><Field label="Street"><input name="street" value={form.street} onChange={change} /></Field><Field label="Purok"><input name="purok" value={form.purok} onChange={change} /></Field></Section><Section title="Employment and education"><Field label="Occupation"><input name="occupation" value={form.occupation} onChange={change} /></Field><Field label="Monthly income bracket"><input name="salary" value={form.salary} onChange={change} /></Field><Field label="Educational level"><input name="educational_level" value={form.educational_level} onChange={change} /></Field><Field label="School name"><input name="school_name" value={form.school_name} onChange={change} /></Field><Field label="Course / degree"><input name="course" value={form.course} onChange={change} /></Field><Field label="Year level"><input name="year_level" value={form.year_level} onChange={change} /></Field><Field label="Graduation year"><input name="graduation_year" value={form.graduation_year} onChange={change} /></Field></Section><section className="claim-section"><div className="claim-section-title"><h2>Resident status</h2><p>Choose all that apply.</p></div><div className="claim-status-options">{[["currently_enrolled", "Currently enrolled"], ["fourps_beneficiary", "4Ps beneficiary"], ["senior_citizen", "Senior citizen"], ["voter", "Registered voter"]].map(([name, label]) => <label key={name}><input name={name} type="checkbox" checked={Boolean(form[name])} onChange={change} /> {label}</label>)}</div></section><div className="claim-form-actions"><p>Changes take effect only after approval.</p><button disabled={saving}>{saving ? "Sending request..." : "Submit for approval"}</button></div></form></main>;
}
function Section({ title, children }) { return <section className="claim-section"><div className="claim-section-title"><h2>{title}</h2></div><div className="claim-form-grid">{children}</div></section>; }
function Field({ label, children }) { return <label className="claim-field"><span>{label}</span>{children}</label>; }
